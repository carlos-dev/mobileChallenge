import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },

  alignCenter: {
    alignItems: 'center',
  },

  buttonDelete: {
    backgroundColor: '#da434c',
  },
});
